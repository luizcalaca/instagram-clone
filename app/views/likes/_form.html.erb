<%= form_with(model: like, local: true, method: :post) do |form| %>
  <% if like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(like.errors.count, "error") %> prohibited this like from being saved:</h2>

      <ul>
        <% like.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :post_id, value: params[:post_id] %>
    <%= form.label :counter %>
    <%= form.number_field :counter %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
